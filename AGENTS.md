# AGENTS.md

- Repo is currently docs-only: the only tracked files are `README.md` and `.gitignore`.
- `README.md` is the only project description; there are no verified app entrypoints, workspace configs, build scripts, or test scripts yet.
- Do not invent commands. If code or manifests are added later, read the root manifest/workflow files first and use the documented scripts.
- There are no existing repo instruction files today (`AGENTS.md`, `CLAUDE.md`, `.cursor/rules/`, `.cursorrules`, `.github/copilot-instructions.md`).
